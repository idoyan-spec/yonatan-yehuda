# קובצי הקול המקוריים - גדולים מדי לגיט (הגרסאות ל-MP3 נשמרות ב-docs\media)
מוסיקה/
הקלטות/

# זמניים
__pycache__/
*.pyc
~$*
build/_icon.html
Thumbs.db
desktop.ini

# קיצור דרך מקומי
*.url

# --- secrets — never commit (managed by session-backup) ---
node_modules/
.env
.env.*
.DS_Store
.bw-session
*.session
.credentials.json
*credentials*.json
*.pem
*.key
id_rsa*
*token*
*.secret
secrets.*
