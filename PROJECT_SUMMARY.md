# אתר כתבי יונתן יהודה ז״ל — סיכום פרויקט

## תיאור כללי

אתר זיכרון סטטי לזכרו של **יונתן יהודה ינקלוביץ ז״ל** (כ״ז אייר תש״ס / 1.6.2000 —
כ״ו אב תשע״ח / 7.8.2018), המרכז את הכתבים שכתב בגיל 13, הקלטות קול, ועמוד שירים
שמחכה לתוכן.

עיקרון מרכזי: לכל פריט מוצג **תאריך היצירה בעברי ובלועזי, וגילו של יונתן באותו יום**.
התאריכים אינם מוקלדים ידנית — הם נשלפים מתוך המטא-דאטה הפנימי של קובצי ה-Word
(`docProps/core.xml` → `dcterms:created`), כלומר מהרגע שבו יונתן באמת יצר את הקובץ.

**האתר החי:** https://idoyan-spec.github.io/yonatan-yehuda/
**הריפו:** https://github.com/idoyan-spec/yonatan-yehuda

## קבצים עיקריים

| קובץ | תפקיד |
|-------|--------|
| `build/build.py` | סקריפט הבנייה הראשי — סורק תוכן, מייצר את כל עמודי ה-HTML, ה-PDF וה-ZIP |
| `build/config.py` | הגדרות האתר (תאריכי לידה/פטירה, גרסה) + קוריאציה ידנית של כותרות המסמכים |
| `build/docx_read.py` | קורא `.docx` — כולל טיפול בתיבות טקסט של Word שיוצרות כפילויות (`mc:Fallback`) |
| `build/make_icon.py` | מרנדר את `icon.svg` בכרומיום ל-`favicon.ico` ו-`assets/icon.ico` |
| `build/assets/style.css` | כל העיצוב, כולל גיליון סגנון נפרד להדפסה (`@media print`) |
| `build/assets/icon.svg` | האייקון — דף עם להבת נר זיכרון |
| `docs/` | **פלט הבנייה** — האתר עצמו. זו התיקייה שמתפרסמת ב-GitHub Pages |
| `כתבים_גיבוי/` | מקורות ה-Word של הכתבים |
| `מוסיקה/` | הקלטות מקוריות (מוחרג מגיט — 350MB; מומר ל-MP3 לתוך `docs/media/`) |
| `שירים/` | שירים (ריק כרגע) |
| `IMG_9166.JPG` | התמונה של יונתן; נחתכת ומוקטנת אוטומטית בבנייה |

## טכנולוגיות בשימוש

- **Python 3.13** — מנוע הבנייה (אין תלות ב-framework; הכל HTML/CSS סטטי)
- **pyluach** — המרת תאריכים עבריים ↔ לועזיים
- **Playwright (Chromium)** — יצירת קובצי PDF מכל עמוד + רינדור האייקון
- **Pillow** — עיבוד התמונה
- **ffmpeg** — המרת הקלטות (WAV/AMR) ל-MP3 לאתר
- **GitHub Pages** — אחסון, מהענף `main`, תיקיית `/docs`
- גופנים: Frank Ruhl Libre + Assistant (Google Fonts)

## איך להשתמש

### התקנה / הכנה

```powershell
pip install pyluach pillow playwright pymupdf
python -m playwright install chromium
```
בנוסף נדרש **ffmpeg** ב-PATH (קיים דרך chocolatey).

### הרצה

```powershell
cd E:\MAIN_CLAUDE\YONATAN
python build\build.py
```
הבנייה מוחקת ובונה מחדש את `docs\` (חוץ מ-`docs\media\` — קובצי MP3 שכבר הומרו
נשמרים כדי לא להמיר שוב כל פעם).

לתצוגה מקומית לפני העלאה:
```powershell
cd docs
python -m http.server 8099
```
ואז לפתוח http://127.0.0.1:8099

### הוספת תוכן

| מה מוסיפים | לאיזו תיקייה | סוגי קבצים |
|---|---|---|
| כתב חדש | `כתבים_גיבוי\` | `.docx` |
| שיר | `שירים\` | `.docx`, `.txt`, `.md`, `.pdf`, תמונה |
| הקלטה | `מוסיקה\` או `הקלטות\` | `.mp3`, `.wav`, `.m4a`, `.amr`, `.ogg` |

אחר כך מריצים `python build\build.py` — האתר מתעדכן לבד.
לכותרות יפות/כותרות משנה/שיוך לאוסף: להוסיף ערך למילון `DOCS` ב-`build\config.py`.

### פריסה (Deploy)

```powershell
python build\build.py
git add -A
git commit -m "עדכון תוכן"
git push
```
GitHub Pages בונה מחדש תוך 1-3 דקות. בדיקת מצב הבנייה:
```powershell
gh api repos/idoyan-spec/yonatan-yehuda/pages/builds/latest --jq .status
```

**מספר הגרסה** מוגדר ב-`build\config.py` (`BUILD`) ומופיע בתחתית כל עמוד באתר —
כך תמיד אפשר לוודא שרואים את הבנייה העדכנית ולא גרסה מהמטמון.

## החלטות תכנון שכדאי לזכור

- **כפילויות טקסט ב-Word**: חלק מהמסמכים נכתבו בתיבות טקסט. Word שומר כל תיבה
  פעמיים (`mc:Choice` + `mc:Fallback`), ולכן חילוץ נאיבי מכפיל את כל התוכן.
  `docx_read.py` מסיר את ענפי ה-Fallback ומייחס כל קטע טקסט לפסקה הקרובה ביותר.
- **`עצמי\כל זמן.docx`** — טיוטה שבה הטקסט פזור בתיבות על פני הדף ולא בסדר קריאה.
  סדר הקריאה הנכון שוחזר לפי משפטים שנקטעים באמצע ומתחברים, ומקודד ב-`order_blocks`
  וב-`join` שב-`config.py`.
- **תאריכי הקלטות**: שמות כמו `160731-004` מקודדים את התאריך ולכן מהימנים. אחרת
  נופלים על חותמת הזמן של הקובץ — וחותמת שנופלת מחוץ לשנות חייו של יונתן נחשבת
  לא-אמינה (תאריך העתקה, לא הקלטה) ומוצג "תאריך לא ידוע". אפשר להשלים ידנית עם
  קובץ `.txt` צמוד (ראו README).
- **מסמכים ריקים** (`גשמים.docx` ושני `דף.docx`) מוחרגים ב-`SKIP`.
- העיצוב מחויב למראה בהיר אחד (נייר חם) — אין ערכת כהה, במכוון.

## היסטוריית שינויים

| תאריך | שינוי |
|--------|-------|
| 2026-08-09 | הקמת הפרויקט: מנוע בנייה, 9 כתבים, 9 הקלטות, עמוד שירים, PDF+ZIP, אייקון וקיצור דרך, פרסום ל-GitHub Pages |
| 2026-08-09 | תיקון תאריכי הקלטות לא-אמינים (חותמות זמן שאחרי הפטירה) + תמיכה בתאריך ידני דרך קובץ `.txt` צמוד |
